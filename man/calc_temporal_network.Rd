% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_temporal_network.R
\name{calc_temporal_network}
\alias{calc_temporal_network}
\title{An automated function to caluclate temporal network with lagged model}
\usage{
calc_temporal_network(
  object,
  which_data,
  lag,
  stratifications,
  alpha = 1,
  nfolds = 3,
  cols_for_meta,
  names,
  cluster_profile = c("auto", "local", "hpc"),
  num_cores = NULL,
  hpc_libpaths = NULL,
  ...
)
}
\arguments{
\item{object}{S4 object of class metime_analyser}

\item{which_data}{dataset or datasets to be used}

\item{lag}{which lagged model to use. 1 means one-lagged model, similarly 2,3,..etc}

\item{stratifications}{List to stratify data into a subset. Usage list(name=value)}

\item{alpha}{parameter for regression coefficient. Set to 1 for lasso regression}

\item{nfolds}{nfolds parameter for glmnet style of regression. Default is set to 3}

\item{cols_for_meta}{a list of character vectors of column names to be used for visualization of the networks.}

\item{names}{character vector with the same length as that of possible models}

\item{cluster_profile}{Character string controlling worker initialization profile.
One of \code{"auto"}, \code{"local"}, or \code{"hpc"}.
\itemize{
\item \code{"auto"}: detects common scheduler environments (e.g. SLURM/PBS/LSF) and chooses \code{"hpc"} when detected.
\item \code{"local"}: standard local machine setup with default library paths.
\item \code{"hpc"}: cluster-oriented setup; can prepend worker library paths via \code{hpc_libpaths}.
}}

\item{num_cores}{numeric input to define the number of cores that you want to use for parallel computing. Default is set to NULL which is parallel::detectCores() -1.}

\item{hpc_libpaths}{Optional character vector of library paths to prepend on worker nodes
when \code{cluster_profile = "hpc"}. Use this when compute nodes need explicit \code{.libPaths()}
(e.g. non-shared user libraries). Ignored for \code{"local"} profile.}

\item{...}{additional arguments for cv.glmnet function}
}
\value{
S4 object with updated temporal network results
}
\description{
calculates temporal networks for each dataset with a lagged model as used in graphical VAR
}
