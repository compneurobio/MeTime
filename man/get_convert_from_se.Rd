% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_convert_from_se.R
\name{get_convert_from_se}
\alias{get_convert_from_se}
\alias{get_convert_from_se,list-method}
\alias{get_convert_from_se,MultiAssayExperiment-method}
\title{Convert SummarizedExperiment objects back to metime_analyser}
\usage{
get_convert_from_se(se, name = NULL, annotations_index = list())

\S4method{get_convert_from_se}{list}(se, name = NULL, annotations_index = list())

\S4method{get_convert_from_se}{MultiAssayExperiment}(se, name = NULL, annotations_index = list())
}
\arguments{
\item{se}{A single \code{SummarizedExperiment} or a list of such objects. When a list
is provided, each entry will be added as a separate dataset to the returned
\code{metime_analyser} object.}

\item{name}{Optional character vector of dataset names. If \code{se} is a list and
\code{name} is \code{NULL}, the names of the list elements will be used. When
\code{se} is a single object and \code{name} is \code{NULL}, the default
dataset name "set_1" will be applied.}

\item{annotations_index}{A named list specifying phenotype and medication datasets.
This mirrors the argument in \code{get_make_analyser_object}. When converting
multiple datasets, the same annotations list will be applied to all.}
}
\value{
An object of class \code{metime_analyser} containing one or more
reconstructed datasets.
}
\description{
This helper reverses the behaviour of \code{get_convert_to_se} by creating a new
\code{metime_analyser} from one or more \code{SummarizedExperiment} objects. It
reconstructs the original data matrix orientation (samples as rows and features as
columns) and retrieves row- and column-level metadata stored in the
\code{colData} and \code{rowData} of the SummarizedExperiment, respectively.
Datasets that lack the mandatory \code{id}, \code{subject} and \code{time} columns in
their row-level metadata will trigger an error, as these fields are required
for a valid \code{metime_analyser}.
}
\section{Functions}{
\itemize{
\item \code{get_convert_from_se(list)}: Convert a list of SummarizedExperiment objects.
When a single dataset has been split by timepoint (i.e. the list contains
multiple SummarizedExperiments whose names share a common prefix before the
final underscore), this method automatically combines them back into a
single dataset by row-binding their sample-level data and taking the
feature-level metadata from the first element.

}}
\examples{
\dontrun{
  # Assume 'se_obj' is a SummarizedExperiment produced by get_convert_to_se()
  analyser <- get_convert_from_se(se_obj, name = "my_dataset")
}
}
