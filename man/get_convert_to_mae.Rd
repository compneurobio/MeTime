% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_convert_to_mae.R
\name{get_convert_to_mae}
\alias{get_convert_to_mae}
\title{Convert a metime_analyser into a MultiAssayExperiment}
\usage{
get_convert_to_mae(
  object,
  which_data = NULL,
  exclude = c("phenotype_data", "medication_data"),
  split_by_time = TRUE
)
}
\arguments{
\item{object}{A \code{metime_analyser} object containing one or more datasets.}

\item{which_data}{Character vector of dataset names to convert. If \code{NULL},
all datasets are converted. Data sets listed in \code{exclude} will be
ignored.}

\item{exclude}{Character vector of dataset names to skip. Defaults to
\code{c("phenotype_data", "medication_data")}.}

\item{split_by_time}{Logical indicating whether longitudinal datasets
should be split by timepoint. Passed through to \code{get_convert_to_se}.
Default is \code{TRUE}.}
}
\value{
A \code{MultiAssayExperiment} containing one \code{SummarizedExperiment} per
selected dataset (or timepoint-specific split). Sample-level metadata is
not merged across datasets, mirroring the structure of the original
analyser object.
}
\description{
This function constructs a \code{MultiAssayExperiment} from the datasets stored in a
\code{metime_analyser}. Each dataset is first converted into one or more
\code{SummarizedExperiment} objects via the \code{get_convert_to_se} helper. When
\code{split_by_time} is \code{TRUE}, longitudinal datasets are split by their
timepoints into separate experiments; otherwise, the full dataset is
preserved in a single \code{SummarizedExperiment} with a time column in the
\code{colData}. Phenotype and medication datasets are skipped by default.
}
\examples{
\dontrun{
  # convert to MAE with splitting by time (default)
  mae <- get_convert_to_mae(humet_object)
  mae

  # convert to MAE without splitting by time
  mae_full <- get_convert_to_mae(humet_object, split_by_time = FALSE)
  mae_full
}
}
