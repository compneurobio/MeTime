% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_convert_from_mae.R
\name{get_convert_from_mae}
\alias{get_convert_from_mae}
\title{Convert a MultiAssayExperiment into a metime_analyser}
\usage{
get_convert_from_mae(mae, name = NULL, annotations_index = list())
}
\arguments{
\item{mae}{A \code{MultiAssayExperiment} object.}

\item{name}{Optional character vector of dataset names corresponding to the
experiments within the MAE. If \code{NULL}, the experiment names will be used.}

\item{annotations_index}{A named list specifying phenotype and medication
datasets, passed through to the underlying conversion routines.}
}
\value{
A \code{metime_analyser} object containing one dataset per experiment in
the input MAE.
}
\description{
This is a convenience wrapper around \code{get_convert_from_se} that converts a
\code{MultiAssayExperiment} (MAE) into a \code{metime_analyser}. Each experiment in the
MAE is first converted into a \code{SummarizedExperiment} and then combined into
a single \code{metime_analyser} with one dataset per experiment. If dataset
names are not provided, the names of the experiments are used.
}
\examples{
\dontrun{
  mae <- get_convert_to_mae(humet_object)
  analyser <- get_convert_from_mae(mae)
}
}
